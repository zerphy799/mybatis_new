<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xzit.mapper.PermissionMapper">

    <select id="findPermByRid" resultType="permission">
        select p.pname ,p.url
        from mybatis.role_permison rp join mybatis.permison p on p.pid = rp .pid where rp.rid= #{rid};
    </select>
</mapper>